<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Bank Page</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-9ndCyUaIbzAi2FUVXJi0CjmCapSmO7SnpJef0486qhLnuZ2cdeRhO02iuK6FUUVM" crossorigin="anonymous">
    <style>
        .error-message {
            color: red;
            font-weight: bold;
        }
    </style>
</head>
<body>
    <div class="jumbotron">
        <div class="container">
          <i class="fas fa-key fa-6x"></i>
          <h1 class="display-3"><%= firstname %>'s Pocket Money</h1>
          <p class="lead">BRB - The best bank in Bray Road</p>
          <hr>
          <% if (error && error.length > 0) { %>
            <% if (error[0].includes('Successfully')) { %>
              <div class="alert alert-success" role="alert">
                <%= error[0] %>
              </div>
            <% } else { %>
              <div class="alert alert-danger error-message" role="alert">
                <%= error[0] %>
              </div>
            <% } %>
          <% } %>
          
          <% if (money !== null) { %>
            <h2>You have R<%= parseFloat(money).toFixed(2) %> in your account.</h2>
          <% } else { %>
            <h2>Unable to display account balance.</h2>
          <% } %>
          
          
          <br>
          <br>
          <a href="/logout" class="btn btn-danger">Logout</a> <!-- Logout button -->
          <a href="/transact" class="btn btn-primary">Banker admin</a> <!-- Transact button -->
          <a href="/withdraw" class="btn btn-warning">Withdraw</a> <!-- Withdraw button -->

        </div>
    </div>
</body>
</html>
